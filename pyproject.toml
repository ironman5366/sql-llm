[project]
name = "sql-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "accelerate>=1.13.0",
    "duckdb==1.5.2",
    "fastapi>=0.136.1",
    "ipykernel>=7.2.0",
    "pydantic>=2.13.3",
    "sglang>=0.5.2",
    "torch>=2.11.0",
    "transformers>=5.7.0",
    "trl>=1.3.0",
    "uvicorn>=0.46.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
