[project]
name = "sql-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "accelerate>=1.13.0",
    "sglang>=0.5.2",
    "torch>=2.11.0",
    "transformers>=5.7.0",
]
